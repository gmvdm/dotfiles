startup_message off
hardstatus on
hardstatus alwayslastline "%{=b}[%c]%{G} Screen(s): %{b}%w"
defscrollback 5000

# Use C-t instead of C-a as this makes more sense for Emacs
# escape ^ta

# Detach on hangup
autodetach on

# Setup default screens
screen -t console